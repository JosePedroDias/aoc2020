import org.junit.Assert
import org.junit.Test
import d07.parseRule

class Day07Test {
    @Test
    fun parseRule() {
        //Assert.assertEquals(parseRule("pale orange bags contain 4 striped gray bags."), null)

        //parseRule("pale orange bags contain 4 striped gray bags.")
        //parseRule("dotted magenta bags contain 2 faded cyan bags, 4 bright yellow bags.")
        //parseRule("posh purple bags contain 2 mirrored lavender bags, 4 light chartreuse bags, 3 shiny green bags.")
        //parseRule("posh fuchsia bags contain 1 striped coral bag, 2 wavy gray bags, 3 posh white bags, 1 mirrored coral bag.")
    }
}